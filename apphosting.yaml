# This file is for Firebase App Hosting.
# Since the project is deployed on Vercel, this file is not actively used
# for the main deployment but is kept for reference or potential future use.
# https://firebase.google.com/docs/app-hosting/configure

runConfig:
  maxInstances: 1
